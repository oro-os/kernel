[package]
name = "oro-kernel-build"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
clap = { version = "*", features = ["derive", "string"] }
toml = "*"
serde = { version = "*", features = ["derive"] }
