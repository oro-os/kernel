[package]
name = "example-spin"
description = "Example module that spins endlessly"
version = "0.0.0"
publish = false
edition = "2021"

build = "build.rs"

[dependencies.oro]
path = "../../../oro"
features = ["runtime"]

[build-dependencies.oro]
path = "../../../oro"
features = ["build"]
default-features = false
