# Procedural Macros for the Oro Kernel Testing Framework

This crate contains the procedural macros used by the Oro kernel testing framework.
You should not depend on this crate directly; instead, use the `orok-test` crate,
which re-exports all of the macros in this crate.
