# Oro Kernel Procedural Macros
This crate implements a number of procedural macros ("proc macros") for the
Oro Operating System kernel.

> [!IMPORTANT]
> **Do not use this library directly.** Instead, consume these via the
> `orok-macro` crate.
