[package]
name = "orok-limine"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
limine.workspace = true
oro-ramdisk = { workspace = true, features = ["decoder", "decompression"] }

[lints]
workspace = true
