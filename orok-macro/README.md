# Oro Kernel Macros
This crate provides a number of macros (both procedural "proc macros" and `macro_rules!`),
along with supporting types.

This crate re-exports all of `orok-macro-proc` macros which should be used via this crate
rather than relying directly on `orok-macro-proc`.
