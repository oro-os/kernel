[package]
name = "orok-util-common"
description = "Shared library for the Oro kernel build/packaging utilities"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
toml.workspace = true
serde = { workspace = true, features = ["derive"] }
